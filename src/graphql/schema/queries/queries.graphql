# import User from "../types/user.graphql"
# import Problem from "../types/problem.graphql"
# import UserSolution from "../types/problem.graphql"
# import GetAllProblemsResponse from "../types/problem.graphql"
# import Topic from "../types/topic.graphql"

# import GetAllProblemsFilterInput from "../inputs/problem_inputs.graphql"

type Query {
  user(id: ID!): User
  profile: User
  users: [User]
  problem(id: ID, slug: String): Problem
  problems(filters: GetAllProblemsFilterInput): GetAllProblemsResponse
  userSolution(id: ID!): UserSolution
  topics: [Topic]
}
